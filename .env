# Local SQLite database file (lives at prisma/dev.db). Data survives across restarts.
DATABASE_URL="file:./dev.db"

# Disable Next.js telemetry (its cross-device config write fails on this machine).
NEXT_TELEMETRY_DISABLED=1
